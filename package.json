{
  "name": "sky",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "uglifyjs-folder src -p **/*.js -o ./dist/foo.js -c -m",
    "start": "npm-watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "watch": {
    "build": "{src,test,src/lib,src/game}/*.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "npm-watch": "^0.11.0",
    "uglify-js": "^3.17.4",
    "uglifyjs-folder": "^3.1.4"
  }
}
